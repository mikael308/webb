<?php
$view_user = \Web\Database\Read::forumUserById(getReqUser());

if ($view_user != null) {
  echo \Web\Helper\setTitle($view_user->getName());
} else {
  echo \Web\Helper\setTitle("could not find user");
}

echo Web\Helper\getStylesheet("user.css");
