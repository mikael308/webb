<?php

# subpages that is not restricted


#TODO if request u is not set, display current user
$view_user = Web\Database\Read::forumUser(
    getReqUser()
);

if ($view_user != null) {
    $subpage = getSubpage() != null ? getSubpage() : '';

    \Web\Framework\Factory\Widgets::addToPage($subpage, 'user');

} else {
    throw new \Exception("user was not found");
}
