<?php

if (\Web\Framework\Session\Security\loginListener()) {
    $redirect_url = $_SESSION['redirect_url'] ?? $GLOBALS['pagelink']['index'];
    $_SESSION['redirect_url'] = '';
    header('Location: ' . $redirect_url);
    exit();
}
\Web\Framework\Session\Security\logoutListener();

