<?php

function getSearchVal()
{
    return isset($_REQUEST['value'])
        ? $_REQUEST['value']
        : null;
}

function getSearchType()
{
    return isset($_REQUEST['type'])
        ? $_REQUEST['type']
        : null;
}
