<?php

if (\Web\Session\Security\loginListener()) {
    $pagelink = $GLOBALS['pagelink']['index'];
    header("Location: $pagelink");
    exit();
}

if (\Web\Session\Security\Authorizer::userIsAuthorized()) {
    $pagelink = $GLOBALS['pagelink']['index'];
    header("Location: $pagelink");
    exit();
}

$_SESSION['registeruser_errmsg'] = '';
if ($_SERVER['REQUEST_METHOD'] == 'POST') {
    # listen for user register post
    if (isset($_POST['user_register'])) {
        \Web\Pages\Register\Helper\register();
    }
}
